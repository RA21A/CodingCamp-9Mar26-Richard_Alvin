{
  "name": "productivity-dashboard",
  "version": "1.0.0",
  "description": "A client-side productivity dashboard with time-based greeting, Pomodoro timer, task manager, and quick links",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "jsdom": "^23.0.0",
    "fast-check": "^3.15.0"
  }
}
